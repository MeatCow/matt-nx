import { generatedBeApiTypes } from './generated-be-api-types';

describe('generatedBeApiTypes', () => {
  it('should work', () => {
    expect(generatedBeApiTypes()).toEqual('generated-be-api-types');
  });
});
